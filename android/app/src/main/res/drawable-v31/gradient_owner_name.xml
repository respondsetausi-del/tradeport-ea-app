<!-- res/drawable/gradient_owner_name.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bottom transparent layer (background) -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent" />
            <corners android:radius="14dp" />
        </shape>
    </item>

    <!-- Gradient layer (glass effect) -->
    <item android:bottom="1dp" android:left="2dp" android:right="2dp" android:top="2dp">
        <shape>
            <gradient
                android:type="radial"
                android:centerX="50%"
                android:centerY="50%"
                android:gradientRadius="99%"
                android:startColor="#000"
                android:endColor="#000" />
            <corners android:radius="24dp" />
        </shape>
    </item>

    <!-- Top transparent layer (outline) -->
    <item>
        <shape>
            <stroke android:width="0.2dp" android:color="@android:color/system_neutral2_600" />
            <corners android:radius="18dp" />
        </shape>
    </item>
</layer-list>