<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/red"/> <!-- Default color -->

    <!-- Adding a black outline with a width of 2dp -->
    <stroke
        android:color="@android:color/black"
        android:width="2dp"/>
</shape>
