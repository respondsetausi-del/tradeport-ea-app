<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Glow ring wrapper for rotating neon border around floating icon -->
    <FrameLayout
        android:id="@+id/icon_glow_ring"
        android:layout_width="68dp"
        android:layout_height="68dp"
        android:layout_marginTop="39dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:padding="6dp">

        <!-- Glass orb container for floating icon -->
        <FrameLayout
            android:id="@+id/icon_container"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_floating_icon_circle">

            <ImageView
                android:id="@+id/icon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                android:src="@mipmap/ic_launcher_foreground"/>
        </FrameLayout>
    </FrameLayout>
    <TextView
        android:visibility="gone"
        android:id="@+id/note_text"
        android:layout_marginTop="40dp"
        android:layout_toEndOf="@+id/icon_glow_ring"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:background="@drawable/layoutback"
        android:textColor="@color/blue_dark"
        android:padding="5dp"
        android:textSize="16sp"
        android:text="hello there"
        />

    <RelativeLayout
        android:id="@+id/autotrade"
        android:layout_width="2000dp"
        android:layout_height="2000dp"
        android:layout_centerInParent="true"
        android:background="@color/black"
        android:padding="5dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/appname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:background="@color/black"
            android:textSize="25dp"
            android:textStyle="bold" />

        <androidx.core.widget.NestedScrollView
            android:id="@+id/sv"
            android:descendantFocusability="blocksDescendants"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_above="@+id/force_stop">
            <TextView
                android:id="@+id/log_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/appname"
                android:layout_marginTop="20dp"
                android:textColor="@color/white"
                android:layout_marginBottom="40dp"
                android:textSize="14sp" />
        </androidx.core.widget.NestedScrollView>




        <Button
            android:id="@+id/force_stop"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_glass_button_danger"
            android:text="FORCE STOP TRADING"
            android:textColor="@color/white"
            android:padding="14dp" />
    </RelativeLayout>

</RelativeLayout>